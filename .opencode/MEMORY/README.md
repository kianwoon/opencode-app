# Project-Local Memory (opencode)

Routing rules: see global `~/.config/opencode/MEMORY/README.md`. Default-write here (project-local); promote to global only if reusable across projects.
Lessons that belong here: `packages/stats` function→server naming vs stale README, `packages/session-ui` conventions, and other repo-specific gotchas.
Format: `lessons/YYYY-MM-DD-<topic>.md` with symptom / root cause / fix / acceptance gate.
